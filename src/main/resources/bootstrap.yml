spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: fast-rest
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:nacos.xxxx.com:80}
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      config:
        file-extension: yml
        namespace: ${NACOS_NS:0d018fcd-951b-4af5-8775-f045ad45a834}
